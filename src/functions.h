#ifndef FUNCTIONS_H
#define FUNCTIONS_H

const auto phi = [](const double x, const double y) {
    return sin(x);
};

const auto f = [](const double x, const double y) {
    return -sin(x);
};

#endif //FUNCTIONS_H
